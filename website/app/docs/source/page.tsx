import DocArticle from "@/components/docs/DocArticle";
export default function SourcePage() { return <DocArticle title="Built in the open." description="Read the source, run Astral locally, and help improve the workspace." sections={[{ id: "repository", title: "The source" }, { id: "development", title: "Local development" }, { id: "contribute", title: "Contributing" }, { id: "gitbook", title: "GitBook publishing" }]} previous={{ href: "/docs/faq", title: "Frequently asked questions" }}>
    <section id="repository"><h2>The source</h2><p>The app combines a Rust/Tauri database core with a React interface. The website lives in its own <code>website/</code> folder.</p><a className="cta-secondary" href="https://github.com/Kraksz/AstralSQL" target="_blank" rel="noreferrer">View on GitHub ↗</a></section>
    <section id="development"><h2>Local development</h2><p>Install Node.js 24, Rust, and your platform’s <a href="https://v2.tauri.app/start/prerequisites/" target="_blank" rel="noreferrer">Tauri prerequisites</a>, then:</p><pre><code>{"git clone https://github.com/Kraksz/AstralSQL.git\ncd AstralSQL\nnpm ci\nnpm test\nnpm run desktop"}</code></pre><p>To run only the SQLite browser workspace, use <code>npm run dev</code>. For the website, run <code>npm run build</code> and <code>npm run website:sync</code> from the root, then <code>npm ci</code> and <code>npm run dev</code> inside <code>website/</code>.</p></section>
    <section id="contribute"><h2>Contributing</h2><p>Read <code>CONTRIBUTING.md</code>, <code>SECURITY.md</code>, and the release checklist before opening a pull request. Include a reproducible test for database behavior changes. Never commit database credentials, local data files, signing keys, or private hosting configuration.</p><p>The included GitHub workflows check the frontend and database core. Windows, Linux, and macOS packaging jobs prepare beta artifacts; actual platform verification is required before publishing installers.</p></section>
    <section id="gitbook"><h2>GitBook publishing</h2><p>This handbook uses a GitBook-style reading layout here on the Astral site. The repository also includes a Markdown handbook and <code>.gitbook.yaml</code> so its owner can connect it to an actual GitBook workspace using Git Sync.</p><p>In GitBook, select the repository and branch, import the repository’s docs, and review before publishing. A GitBook-hosted URL requires the owner’s GitBook workspace. See <a href="https://gitbook.com/docs/getting-started/git-sync" target="_blank" rel="noreferrer">GitBook’s Git Sync guide</a>.</p></section>
  </DocArticle>; }
