{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the local bundled main window may call native app commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
